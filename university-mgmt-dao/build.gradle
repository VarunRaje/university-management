dependencies {
	api deps.springBootStarterDataJpa
	compileOnly deps.lombok
	implementation deps.springBootStarterValidation
	annotationProcessor deps.lombok
	runtimeOnly deps.postgresql
}

jar {
	enabled = true
}
